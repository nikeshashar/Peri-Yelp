<!DOCTYPE html>
<html lang="en">
<head>
  <title>Yelp</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta charset='utf8'>
  <meta name="description" content="Yelp Project">
  <meta name="author" content="Nikesh Ashar">

</head>

<body>
  <div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
      <div class="navbar-header">
      
        <a class="navbar-brand" href="#">Peri-Yelp</a>
      </div>
      <div class="navbar-collapse collapse">
        <form class="navbar-form navbar-right" role="form">
          <% if user_signed_in? %>
          	Logged in as <%= current_user.email %>
          	<%= link_to 'Sign out', destroy_user_session_path, method: :delete, class:"btn btn-md btn-danger", role:"button" %>
          	<% else %>
          	<%= link_to 'Sign in', new_user_session_path, class:"btn btn-md btn-success", role:"button"  %>
          	<%= link_to 'Sign up', new_user_registration_path, class:"btn btn-md btn-warning", role:"button"  %>
          <% end %>
        </form>
      </div><!--/.navbar-collapse -->
    </div>
  </div>

    <div class="jumbotron">
      <div class="container">
        <h1>Yelp!</h1>
        <p>What is this app?.</p> 
      </div>
    </div>


  <%= notice %>
  <%= yield %>

 
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
</body>
</html>

